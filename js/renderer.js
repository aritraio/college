// js/renderer.js - Placeholder for Day 1
window.Renderer = {
  renderSubject: function(subjectId) {
    console.log("Renderer: Renders subject page for " + subjectId);
  },
  renderModule: function(subjectId, moduleId) {
    console.log("Renderer: Renders module " + moduleId + " of subject " + subjectId);
  }
};
